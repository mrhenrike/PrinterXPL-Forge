# (minimum versions are examples - should be adjusted according to real project dependencies)
argparse
python-nmap>=0.7.1
fpdf2>=2.6.1
pyyaml>=6.0
termcolor>=2.4.0

# HTTP/IPP/HTTPS capabilities
requests>=2.0
urllib3>=1.25

# SNMP support (discovery e capabilities via pysnmp)
pysnmp>=4.4
pyasn1>=0.4

# Console UI (opcional, apenas se usar console.py)
npyscreen>=4.10

# (nenhuma outra dependência de terceiros)
