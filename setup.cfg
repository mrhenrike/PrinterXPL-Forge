# setup.cfg — complementa pyproject.toml
# Necessário para declarar módulos Python soltos (não em subpacote)
# que ficam diretamente em src/ (main.py, version.py).

[options]
py_modules =
    main
    version
